# requirements.txt
# HyperMesh/OptiStruct RAG — src/ で実際に import される依存のみ

# ==================== UI ====================
streamlit>=1.56.0

# ==================== LangChain ====================
langchain>=1.2.15
langchain-core>=1.3.0
langchain-community>=0.4.1
langchain-classic>=1.0.4
langchain-chroma>=1.1.0
langchain-openai>=1.1.15
langchain-huggingface>=1.2.2
langchain-text-splitters>=1.1.2

# ==================== Vector store & BM25 ====================
chromadb>=1.5.8
rank-bm25>=0.2.2

# ==================== Embeddings (HuggingFace) ====================
huggingface-hub>=1.11.0
sentence-transformers>=3.3.0
# PyTorch: 標準 pip では CPU ビルドのことが多い。NVIDIA GPU で埋め込みを高速化する場合は
# https://pytorch.org/ の「Install」に従い、CUDA 対応 wheel を入れてください（コードは cuda 利用可能時に自動選択）。
torch>=2.5.0

# ==================== LLM API ====================
openai>=2.32.0

# ==================== Config & HTML ====================
python-dotenv>=1.2.2
beautifulsoup4>=4.14.3
requests>=2.32.5

# ==================== graph_retrieval (BaseRetriever) ====================
pydantic>=2.10.0
